import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const teacherManagementSlice = createSlice({
    name: 'teacherManagement',
    initialState,
    reducers: {

    }
})

export const {

} = teacherManagementSlice.actions

// Selectors
// export const selectteacherManagements = (state) => state.teacherManagement.teacherManagements


export default teacherManagementSlice.reducer